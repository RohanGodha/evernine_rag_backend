fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0
groq==0.13.1
python-dotenv==1.0.1
pytest==8.3.4
httpx==0.28.1

# Vector RAG is OFF by default and NOT needed for this task (enrichment, not
# Q&A over external knowledge). Its heavy deps live in requirements-rag.txt and
# are only required if you set RAG_ENABLED=true.
